		;Considerei	que o endereço do vetor esta em R0 e o tamanho dele em R1
		
		;O		visUAL apontou erro quando eu cliquei em Execute. Porem, funcionou corretamente com
		;		Step	Forwards no mesmo caso de teste. Por isso, use Step Forwards para testar o codigo
		
		;		A funcao percorre o vetor i vezes contando a quantidade de ocorrencias de cada elemento
		;		e para de percorrer quando encontra um elemento com uma unica ocorrencia.
		
		
		;		Esse e um caso de teste gerado para testar a funcao
ARRAY	DCD		1, 9, 1, 2, 8, 7, 7, 8, 2
		LDR		R0, =ARRAY
		MOV		R1, #36
		
		
		;		A funcao comeca aqui
QUESTAO1
		STMFD	SP!, {R4, R5, R6}
		MOV		R4, #0 ; i = 0
		MOV		R3, #0 ;Caso o lixo de memoria em R3 seja igual a 1, nao deixa entrar no DONE
		
WHILEEXT
		;if(count	== 1) termina a funcao
		CMP		R3, #1
		BEQ		DONE
		
		LDR		R2, [R0, R4] ;atual = v[i]
		ADD		R4, R4, #4   ;++i;
		MOV		R3, #0	   ;count = 0
		MOV		R5, #0	   ;j = 0
		
WHILEINT
		;while(j	< n)
		CMP		R5, R1
		BHS		WHILEEXT
		
		CMP		R3, #2
		BHS		WHILEEXT
		
		;if(v[j]	== atual] ++count;
		LDR		R6, [R0, R5]
		CMP		R6, R2
		ADDEQ	R3, R3, #1
		
		;j++
		ADD		R5, R5, #4
		B		WHILEINT
		
DONE
		;return	atual
		MOV		R0, R2
		LDMFD	SP!, {R4, R5, R6}
		MOV		PC, LR
